Simple Genealogy Tree application written in React.

## Running
Enter the following command inside the root directory:

`npm start`

## Usage
To have your own tree displayed, either copy and paste the json into
the text area below the tree, or put it in the `tree.json` file in the
src directory.